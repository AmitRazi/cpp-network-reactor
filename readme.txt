This program was written and tested on Ubuntu 22.04LTS.

To run the chat server, run the make and then type ./react_server in the linux shell.

To connect to the chat server, use the command "telnet 127.0.0.1 9034" or use the poll client in the course moodle.

